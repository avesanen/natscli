```
NAME:
   NatsCLI - Commandline interface for nats.

USAGE:
   natscli [global options] command [command options] [arguments...]

VERSION:
   0.0.0

COMMANDS:
     publish, pub, p    publish
     subscribe, sub, s  subscribe
     request, req, r    request
     help, h            Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --url value    (default: "nats://localhost:4222")
   --help, -h     show help
   --version, -v  print the version
flag provided but not defined: -?
```
